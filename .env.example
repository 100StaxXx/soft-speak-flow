# Supabase Configuration
# Project Settings → API → Project URL / anon (publishable) key
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-or-publishable-key

# Edge Function Secrets (set via Supabase CLI, not checked into git)
# supabase secrets set --project-ref your-project-ref SUPABASE_SERVICE_ROLE_KEY=your-service-role
# supabase secrets set --project-ref your-project-ref SUPABASE_ANON_KEY=your-anon-key

# (Optional) handy for scripts, not consumed by app code
VITE_SUPABASE_PROJECT_ID=your-project-ref

# Google OAuth Configuration
# Create OAuth client IDs at https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_WEB_CLIENT_ID=your-web-client-id.apps.googleusercontent.com
VITE_GOOGLE_IOS_CLIENT_ID=your-ios-client-id.apps.googleusercontent.com

# Native Apple auth uses Supabase secrets for the private key.
# Do NOT store Apple private keys or Supabase service-role keys in .env files.
