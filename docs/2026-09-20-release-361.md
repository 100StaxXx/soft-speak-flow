# Cosmiq 4.8 (361)

## Included

- Pathfinder matches the quiet, dark quest editor across its goal, timeline, ritual and review steps. Compact labeled progress, consistent portal controls and pinned actions replace the pale mint framing. Calendar-day deadlines and touch-visible ritual actions are corrected; the campaign name is always editable and “why” is optional; failed saves preserve the plan with a retry message.
- Google/Outlook native calendar launch retries once after clearing a retained Safari controller, using the same validated authorization URL. Overlapping launches are blocked. No login/session/token changes.
- Connected tasks and reminders move from Goals to Settings → Preferences, under calendar integrations.
- Companion prompt version 4 requests more visible whole-body movement while retaining the scenic background and returning to the original pose. Four idle clips remain 4 seconds each; Mind/Body/Soul remain 5 seconds each. Existing v3 clients can still read saved videos.

## Backend

- Deployed only `companion-wellbeing-video`, v5 → v6 ACTIVE, with unchanged `verify_jwt=false` and handler-level authentication. Before/after inventories show no other changed function versions.
- Live runtime check: unauthenticated 401; authorized 200 with 42 habitats and a 223,678-byte composed frame.
- No migration, account/trial grant, stored-media deletion, auth change, or manual paid regeneration. The temporary Colima runtime was stopped afterward.

## Checks

- Final combined run: 88 tests across 15 files passed, covering Pathfinder/ritual/deadline/campaign behavior, calendar recovery/settings/URL handling, companion motion and Settings placement. Another 30 backend tests passed, including v3 retention, v4 idempotency, scene preparation, access checks and video-provider handling.
- Typecheck, affected-file lint, production build, bundle budgets, asset checks and iOS app/widget preflight passed.
- Real Pathfinder components checked with synthetic data at 390×844, 320×667 and 1024×800. This does not verify production AI responses or native voice input. Temporary preview files were removed.
- Logs: `/private/tmp/cosmiq-release-361.CmMIal`.

## Delivery

- Signed archive succeeded: `/Users/macbookair/Library/Developer/Xcode/Archives/2026-09-20/Cosmiq 4.8 (361) 2026-09-20.xcarchive`.
- All 476 archived web files match the final `dist` exactly. The native signature verified successfully and the app reports build 361.
- Removed only build 361's completed DerivedData compiler cache (~977 MB) to make room for export. It is regenerable; signed archives, source, media and personal files remain intact.
- Xcode upload succeeded September 20, 2026 at 16:53 PDT. Apple completed processing; the existing internal Test Group 1 (two testers) lists **4.8 (361) — Testing**. What to Test notes were saved and verified. No external beta or App Store review was submitted.
- App Store Connect build ID: `deb7144a-a771-447d-882e-0c5c097967fb`.

## Device acceptance

Verify real Google/Microsoft consent and calendar synchronization on iPhone. New video output needs visual acceptance after eligible accounts prepare v4 clips; stronger prompts cannot guarantee generated motion quality, and existing generation limits may delay new clips. No claim of live end-to-end AI, calendar consent or voice-input testing is made.
