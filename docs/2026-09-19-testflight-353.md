# Cosmiq 4.8 (353) — coordinated testing release

Date: September 19, 2026 (Pacific).
App: 6755738842 / com.darrylgraham.revolution.

## App
Signed Xcode archive succeeded. Signature verification passed. Archive version is 4.8 (353).
Archived web assets match the production build; the only additional public files are Capacitor's cordova stubs.
Upload through Xcode SUCCEEDED at September 19, 2026, 20:34:17 Pacific (September 20, 03:34:17 UTC). Xcode exited 0 and reported EXPORT SUCCEEDED. Apple reports the package is processing; tester availability is not yet verified.

Included: secure native session storage and resume recovery; bounded Apple login transport retries; paywall entitlement-loading safeguards and Apple-derived trial duration; Calendar/Goals cleanup; element/form habitat backgrounds; hatch/evolution presentation and clean companion controls; calendar event details/import and opt-in foreground linking; Google/Outlook task browsing/linking and explicit Google Tasks/Apple Reminders export; multi-calendar display and native recurrence export.

The trial wording comes from Apple's returned offer. A 14-day offer is correctly displayed as 14 days, but the actual subscription offer configuration has not been changed/verified because App Store Connect requires sign-in. Do not claim a live 14-day offer yet.

## Backend deployed
Project opbfpbbqvuksuvmtmssd (shared with Graceward):
- calendar-read-events v2
- calendar-linked-event v1
- calendar-task-items v1
- calendar-sync-worker v1, disabled and unscheduled
- google-calendar-auth v69, fresh live baseline + Cosmiq-only Tasks scope
- check-apple-subscription v59 was verified identical to the local entitlement fix; not redeployed
- outlook-calendar-auth v66 already has Tasks.ReadWrite; not redeployed
- shared campaign generation already has the fallback logic and product-aware guidance; not overwritten

Applied in one transaction after rollback-only production validation:
- 20260919210000_safe_calendar_quest_links
- 20260919210100_calendar_task_export_intents
- 20260919210200_calendar_background_sync

Renumbered the unshipped calendar migrations to avoid collision with the already-deployed hatch migration 20260919140000. Migration history is recorded. Existing task constraint values, including Graceward's faithful_step, were preserved. No existing tasks or calendar connections were rewritten.

Google users must reconnect in Preferences and approve Tasks permission. Outlook users may need renewed consent. Apple Calendar and Reminders require device permissions.

## Checks
- 201 focused frontend release tests passed.
- 159 additional trial/calendar/layout/date tests passed (separate files).
- 51 provider/worker tests passed with mocked provider calls.
- Local PostgreSQL migration and ownership/lease/export isolation suites passed.
- Exact schema migration validated against production in BEGIN/ROLLBACK, then committed separately and verified.
- New endpoints return HTTP 401 without user/internal authentication.
- All 16 deployed source files match the tested release sources. Worker control is false, no worker cron job exists, and the new link/export tables contain zero rows after rollout.
- Apple Auth health, Apple redirect and invalid-token rejection smoke checks passed; these do not prove successful account sign-in on a phone.
- Function typechecks, release preflight, bundle/Maps checks and 97-entry function manifest passed.
- Full-project TypeScript checking was rerun and still reports the same 37 previously documented errors; not represented as passing. See typecheck.log in the release log directory.
- No actual user calendar items were created/edited/deleted during release checks.

## Still excluded / verification required
- Multiple Google/Outlook accounts per provider.
- Complete cross-provider recurring-series editing.
- Provider webhooks/free-busy/travel buffers.
- Automatic closed-app worker activation: service/schema deployed, but disabled pending real-provider consent, conflict and refresh-overlap testing. Apple synchronization requires the device.
- Physical-device Apple login, real purchases/restore, calendar consent and end-to-end provider writes.
- Apple processing/tester distribution and the actual 14-day introductory offer.

## Artifacts
Archive: /Users/macbookair/Library/Developer/Xcode/Archives/2026-09-19/Cosmiq 4.8 (353) 2026-09-19.xcarchive
Logs: /private/tmp/cosmiq-testflight-353.E3zIBZ
Database rollout: /private/tmp/cosmiq-calendar-release.djAbaD
Saved product-safe Google deployment: supabase/hotfixes/20260919-calendar-release

No App Store review submission was requested or made. No commit/push was performed in this release pass.
