# Cosmiq 4.8 (359) release

## iPhone app

- Based on pushed source `f956d10a2`, with app/widget build numbers advanced to 359. Includes the Agenda chat launcher, draft editor handoffs, crisp scenery, restrained task animations, calendar drag/summary interactions, saved view/Inbox preferences, and saved-session recovery.
- Native preflight and archive succeeded. All 476 archived web assets matched the tested `dist` output exactly.
- Xcode upload succeeded September 20, 2026 at 14:13 PDT. Apple completed processing; the existing internal Test Group 1 (two testers) lists **4.8 (359) — Testing**. Focused What to Test notes were saved. No external beta review or App Store review submission was made.
- Signed archive: `/Users/macbookair/Library/Developer/Xcode/Archives/2026-09-20/Cosmiq 4.8 (359) 2026-09-20.xcarchive`.
- Temporary verification logs: `/private/tmp/cosmiq-release-359.K1I47x`.

## Scoped production backend

- Compared live wellbeing v3 and cinema v22 sources plus their dependency closures before deployment. Only the reviewed wellbeing entry/prompt changes and hatch scene endpoint preparation differed (ignoring trailing whitespace in two imported helpers).
- Deployed `companion-wellbeing-video` v4 and `process-companion-cinema-event` v24; both ACTIVE. Before/after inventory confirmed no other function versions changed.
- Initial cinema deployment v23 inherited the CLI's gateway JWT default. The post-deploy metadata check detected this immediately; added explicit configuration and redeployed v24 to preserve the previous `verify_jwt=false` setting. Handler internal-secret authentication remains enforced. Unauthenticated final requests return 401. The gateway mismatch existed briefly during release; no customer-media reads were used to investigate it.
- Deployed wellbeing via the Docker bundler, including all 42 habitat assets and pinned ImageMagick WASM. Its protected `/runtime-check` requires an existing service-role credential and performs no customer-data reads, database writes, storage writes, or provider calls. Live check returned 200, 42 habitats, and a 223,678-byte composed JPEG; unauthenticated check returned 401.
- All 16 downloaded cinema TypeScript sources match the local deployed closure. The wellbeing bundle could not be re-extracted: the server unpacker returned 500 and the local unpacker failed on a directory entry. Do not claim source round-trip parity for that bundle; its deployment result and authenticated live compositor/asset check succeeded.
- No database migrations, Apple settings, account records, entitlement grants, or other shared functions changed. No paid render was manually started. The normal worker continues processing eligible queued work.
- Existing recordings are unchanged. Background continuity on actual newly generated hatch and wellbeing videos still requires device/visual acceptance; a successful compositor check is not a claim that a new video has been watched.

## Checks

- Relevant frontend tests, typecheck, changed-file lint and production build passed before archiving (see Agenda refresh notes).
- 25 focused video tests and five scoped release validator tests passed; backend typechecks and function manifest check passed.
- An additional cinema configuration regression test passed with the two existing entry/endpoint tests, ensuring future deployments explicitly preserve gateway metadata.
- Removed only inactive temporary Xcode exports for builds 352–354 (~1.6 GB) and the completed build-359 compiler cache (~977 MB). Saved signed archives, source, and personal files remain intact. Deleted temporary files can be recreated, not recovered from Trash.

## Reference

The static-file deployment path follows [Supabase's Wasm deployment guidance](https://supabase.com/docs/guides/functions/wasm) and its [ImageMagick example](https://supabase.com/docs/guides/functions/examples/image-manipulation). The deployed runtime smoke check, rather than local compilation alone, verifies package availability.
