name: Frontend Release Gate

on:
  pull_request:
  push:
    branches:
      - main
      - "codex/**"
  workflow_dispatch:

env:
  # Public product identity stays pinned even in CI. Keys below are inert test
  # values: these asset-check builds are never signed, uploaded, or distributed.
  VITE_SUPABASE_URL: https://opbfpbbqvuksuvmtmssd.supabase.co
  VITE_EXPECTED_SUPABASE_PROJECT_REF: opbfpbbqvuksuvmtmssd
  VITE_SUPABASE_PUBLISHABLE_KEY: ci-public-key
  VITE_GOOGLE_WEB_CLIENT_ID: ci-web-client-id
  VITE_GOOGLE_IOS_CLIENT_ID: ci-ios-client-id
  VITE_GOOGLE_MAPS_API_KEY: ci-maps-key
  VITE_NATIVE_REDIRECT_BASE: https://app.cosmiq.quest
  VITE_REVENUECAT_IOS_API_KEY: appl_CIassetchecknotarealkey

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - run: npm ci
      - run: npm run product:verify
      - run: npm run secrets:scan
      - run: npm run lint
      - run: npm run typecheck
      - run: npm run deadcode:check
      - run: npm run test:run
      - run: npm run test:migrations:static
      - run: npm run functions:typecheck
      - run: npm run functions:test
      - run: npm run build
      - run: npm run maps:verify-autocomplete -- dist
      - run: npm run bundle:check

  ios-assets:
    runs-on: macos-15
    needs: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - run: npm ci
      - run: npm run ios:sync:testflight
      - run: npm run ios:verify-widget-signing
